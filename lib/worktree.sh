#!/usr/bin/env bash
# lib/worktree.sh — DEPRECATED. Moved to adapters/workspace/git_worktree.sh.
#
# 신규 코드는 port API (`ws_*`) 를 호출해야 한다.
# adapter 는 lib/registry.sh 가 lib/common.sh 에서 자동 source.
#
# 점진 이전이 끝나면 본 파일은 삭제된다.

return 0 2>/dev/null || true
